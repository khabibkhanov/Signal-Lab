# /add-endpoint

Create a new NestJS endpoint in Signal Lab using repository conventions.

## Command Prompt

Implement a new endpoint under apps/backend/src using DTO validation, PrismaService persistence, structured logging, Prometheus instrumentation, and Swagger decorators. Reuse existing module patterns. Return a short diff summary and verification steps.
