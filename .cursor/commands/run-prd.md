# /run-prd

Execute PRD delivery using orchestrator context pattern.

## Command Prompt

Use the signal-lab-orchestrator skill to process a PRD file path. Create or update .execution/<timestamp>/context.json, run through phases, and return an execution report with completed tasks, failed tasks, retries, and model usage distribution.
